server:
  servlet:
    context-path: /api

spring:

  data:
    redis:
      host:
      port:

  datasource:
    url:
    username:
    password:

  minio:
    url:
    accessKey: minio
    secretKey: minio123

  session:
    timeout: 1m

  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: true
    properties:
      hibernate:
        format_sql: true



cors:
  allowedOrigin: http://localhost
